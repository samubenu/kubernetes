apiVersion: v1
kind: Pod
metadata:
  generateName: pod-victim-
spec:
  priority: 1000000000
  containers:
  - image: registry.k8s.io/pause:3.10.1
    name: pause
    resources:
      requests:
        cpu: 10m
        memory: 10Mi
